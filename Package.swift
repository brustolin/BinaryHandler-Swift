// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "BinaryReader",
    products: [
        .library(
            name: "BinaryReader",
            targets: ["BinaryReader"]),
    ],
    targets: [
        .target(
            name: "BinaryReader",
            dependencies: []),
    ]
)
